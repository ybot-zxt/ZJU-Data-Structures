/*#include<stdio.h>
#include<stdlib.h>

#define MAXSIZE 100
typedef int ElementType;
typedef struct LinearList{
    ElementType Data[MAXSIZE];
    int Last;
} LinearList, *List;

List MakeEmptyList(void);








List MakeEmptyList(void){
    List LPtr = (List)malloc(sizeof(struct LinearList));
    LPtr->Last = -1;
    return LPtr;
}

*/